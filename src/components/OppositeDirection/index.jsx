import React from "react";

export const OppositeDirection = ({ gameArray, addArray, fromSoftware }) => {
  return (
    <div>
      <p>{gameArray}</p>
      <p>{addArray}</p>
      <p>{fromSoftware}</p>
    </div>
  );
};
